import 'package:flutter/material.dart';

class Reminders extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final String id = 'reminders';

    return Scaffold(
      body: Container(child: Text('ja')),
    );
  }
}
